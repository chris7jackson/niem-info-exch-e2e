# Testing dependencies for NIEM API

# Core testing framework
pytest==7.4.3
pytest-asyncio==0.21.1
pytest-cov==4.1.0
pytest-mock==3.12.0
pytest-xdist==3.3.1  # Parallel test execution
pytest-timeout==2.2.0  # Test timeout handling

# HTTP testing
httpx==0.25.2  # Already in main requirements
requests-mock==1.11.0  # Mock HTTP requests

# Database testing with containers
# Note: Using services directly in CI instead of testcontainers for better compatibility
# testcontainers==3.7.1

# Mocking and fixtures
factory-boy==3.3.0  # Object factories for tests
freezegun==1.2.2  # Time mocking
responses==0.24.1  # Mock HTTP responses

# Code quality and linting (development)
ruff==0.1.6  # Fast Python linter
black==24.3.0  # Code formatter
mypy==1.7.1  # Type checking
isort==5.12.0  # Import sorting

# Performance testing
pytest-benchmark==4.0.0

# Security testing
pbr>=5.11.0  # Required by bandit (explicit for better dependency resolution)
bandit[toml]==1.7.9  # Security linter (updated for better pbr compatibility)
# safety removed due to packaging dependency conflict
# Use pip-audit or trivy instead for vulnerability scanning

# Documentation testing
pytest-doctestplus==1.0.0